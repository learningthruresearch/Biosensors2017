# coding=utf8

import serial
import csv
ser = serial.Serial('/dev/ttyACM4', 9600)
fichier = open('90%(3).csv', 'a')
wr = csv.writer(fichier)
for i in range(2000):
    test = str(ser.readline())
    fichier.write(test)
    print(test)
fichier.close()

